# pnpm 11 默认禁止依赖跑构建脚本（esbuild 等需要它来安装平台二进制）
# 用 dangerouslyAllowAllBuilds 一律允许，避免 CI 的 ERR_PNPM_IGNORED_BUILDS
# 注：pnpm 11 的审批字段已从 onlyBuiltDependencies(数组) 改为 allowBuilds(map)
dangerouslyAllowAllBuilds: true
